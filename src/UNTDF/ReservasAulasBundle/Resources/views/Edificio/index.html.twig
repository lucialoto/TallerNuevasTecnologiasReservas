{% extends '::base.html.twig' %}

{% block body %} 
    <a href="{{path('edificio/agregar')}}">AGREGAR</a>
    {% for edificio in edificios %}
        <p>   
            <a href="{{path('edificio/ver',{'id':edificio.id})}}">{{ edificio.id }}</a> {{ edificio.nombre}}
            <a href="{{path('edificio/edit',{'id':edificio.id})}}">UPDATE</a>
            <a href="{{path('edificio/borrar',{'id':edificio.id})}}">BORRAR</a>
        </p>
    {% endfor %} 
{% endblock %}


