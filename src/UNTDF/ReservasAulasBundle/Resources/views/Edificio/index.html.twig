{% extends '::base.html.twig' %}

{% block body %} 
    {% for edificio in edificios %}
        <p> {{ edificio.id }} {{ edificio.nombre}} 
            <a href="{{path('edificio',{'id':edificio.id})}}">VER</a>
            <a href="{{path('edificio/borrar',{'id':edificio.id})}}">BORRAR</a>
        </p>
    {% endfor %} 
    <a href="{{path('edificio/agregar')}}">AGREGAR</a>
{% endblock %}


