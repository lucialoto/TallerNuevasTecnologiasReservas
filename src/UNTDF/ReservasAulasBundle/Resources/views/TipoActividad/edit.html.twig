{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TipoActividad edit</h1>

    {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}
        
        <div>
            {{ form_label(edit_form.nombre) }}
            {{ form_errors(edit_form.nombre) }}
            {{ form_widget(edit_form.nombre) }}
        </div>
        <p>----------</p>
        <div>
            {{ form_label(edit_form.cantidadHoras) }}
            {{ form_errors(edit_form.cantidadHoras) }}
            {{ form_widget(edit_form.cantidadHoras) }}
        </div>
        <p>----------</p>
        
        
        {{ form_end(edit_form) }}

    <li>
        <a href="{{ path('tipoactividad') }}">
            Volver a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
