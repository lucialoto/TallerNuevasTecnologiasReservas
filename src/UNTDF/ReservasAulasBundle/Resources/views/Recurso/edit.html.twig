{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Recurso - Edición</h1>

    {{ form(edit_form) }}

        
        <a class="btn btn-primary" href="{{ path('recurso') }}"> <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
            Volver a la lista
        </a>
        
    {{ form(delete_form) }}

{% endblock %}
