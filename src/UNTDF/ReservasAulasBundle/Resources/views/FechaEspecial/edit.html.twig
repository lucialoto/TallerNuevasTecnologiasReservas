{% extends '::base.html.twig' %}

{% block stylesheets -%}
    <link rel="stylesheet" tyep="text/html" href="{{ asset('bundles/untdfreservasaulas/vendor/bootstrap-datepicker/css/bootstrap-datepicker3.standalone.min.css')}}" />
{% endblock %}

{% block body -%}
    <h1>Fechas Especiales - Edición</h1>

    {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}
        
        <div>
            {{ form_label(edit_form.fecha) }}
            {{ form_errors(edit_form.fecha) }}
            {{ form_widget(edit_form.fecha) }}
        </div>
        
        <p>----------</p>
        
        <div>
            {{ form_label(edit_form.descripcion) }}
            {{ form_errors(edit_form.descripcion) }}
            {{ form_widget(edit_form.descripcion) }}
        </div>
        
        <p>----------</p>
                
        {{ form_end(edit_form) }}

    
        <a class="btn btn-primary" href="{{ path('fechaespecial') }}"> <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span>
            Volver a la lista
        </a>
    
    {{ form(delete_form) }}
</ul>
{% endblock %}
{% block javascripts -%}
    <script src="{{ asset('bundles/untdfreservasaulas/vendor/bootstrap-datepicker/js/bootstrap-datepicker.min.js')}}"></script>
    <script src="{{ asset('bundles/untdfreservasaulas/vendor/bootstrap-datepicker/locales/bootstrap-datepicker.es.min.js')}}"></script>
    <script type="text/javascript">
        $('.fechareservada_input_text').datepicker({
            startDate: "{{ "now"|date('Y-m-d')}}",
            endDate: "{{ "now"|date_modify('+1 Year')|date('Y-m-d')}}",
            todayBtn: "linked",
            format: "yyyy-mm-dd",
            clearBtn: true,
            language: "es",
            orientation: "top auto",
            autoclose: true
        });
    </script>
{% endblock %}
