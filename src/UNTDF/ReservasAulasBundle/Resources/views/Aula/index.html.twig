{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Aulas</h1>
    <p><a class="btn btn-primary" href="{{ path('aulas_new') }}"><span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> AGREGAR</a>
    </p>    
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('aulas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>
                
                        <a href="{{ path('aulas_show', { 'id': entity.id }) }}">show</a>
                    
                        <a href="{{ path('aulas_edit', { 'id': entity.id }) }}">edit</a>
                    
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        
    {% endblock %}
