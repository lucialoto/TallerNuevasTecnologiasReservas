{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Aulas</h1>
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="alert alert-success flash-notice">
            <span aria-hidden="true">{{ flashMessage }}</span>
        </div>
    {% endfor %}
    <p><a class="btn btn-primary" href="{{ path('aulas_new') }}">
        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> AGREGAR
        </a></p>    
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Edificio</th>
                <th>Nombre</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.edificio }}</td>
                <td>{{ entity.nombre }}</td>
                <td>
                
                    <a href="{{ path('aulas_edit', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-pencil"></span> EDITAR</a>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        
    {% endblock %}
