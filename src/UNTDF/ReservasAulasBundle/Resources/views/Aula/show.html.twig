{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Aula</h1>
    <h3>Id <small>{{ entity.id }}</small></h3>
    <h3>Nombre <small>{{ entity.nombre }}</small></h3>
    

     <fieldset class="buttons">
        <a class="btn btn-primary" href="{{ path('aulas') }}"><span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span> Lista</a>
    
        <a class="btn btn-primary" href="{{ path('aulas_edit', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar</a>
     </fieldset>
        {{ form(delete_form) }}
    

{% endblock %}
